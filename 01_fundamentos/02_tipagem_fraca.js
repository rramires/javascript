// declaracao, poderia usar var tambem
let qualquer

// typeof testa o tipo
console.log(qualquer, typeof qualquer) // undefined, quando não tem valor definido

// atribuindo string
qualquer = 'Ricardo'
console.log(qualquer, typeof qualquer) // string

// atribuindo numero
qualquer = 12
console.log(qualquer, typeof qualquer) // Number

// atribuindo numero, nao tem int, float(ponto flutuante), etc tudo nunber
qualquer = 3.14
console.log(qualquer, typeof qualquer) // Number
